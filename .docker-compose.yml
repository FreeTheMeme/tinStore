
services:
  
  db-2:
    image: mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: example
    ports:
      - "3306:3306"  # Maps port 3306 on the host to port 3306 in the container